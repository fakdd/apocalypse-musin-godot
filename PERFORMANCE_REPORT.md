# PERFORMANCE_REPORT — 최적화 전/후 성능 비교

## 측정 방법 (정직한 A/B)

같은 빌드에서 **`WorldSystem.BATCH_ENABLED` 플래그만 토글**해 두 번 실행했다.
코드를 되돌리지 않고 A/B 를 재현할 수 있어야 측정이 신뢰할 수 있기 때문이다.

```bash
BENCH_TAG=BEFORE BENCH_BATCH=0 godot --path . --resolution 1280x720
BENCH_TAG=AFTER                 godot --path . --resolution 1280x720
```

| 조건 | 값 |
|---|---|
| 해상도 | 1280×720, Forward+ (Vulkan) |
| 월드 시드 | `seed(20260726)` 고정 |
| 부하 | 적 44기(5종 혼합) 동시 활동 + 밤 페이즈 |
| 샘플 | 120 프레임 |
| 프레임 시간 | `Time.get_ticks_usec()` 실측 (중앙값·p95) |
| 주의 | 측정 루프에서 `CombatFeel.reset()` 을 호출해 히트스톱의 `time_scale` 이 CPU 지표를 왜곡하지 않게 했다 |

---

## 결과

| 지표 | BEFORE | AFTER | 변화 |
|---|---:|---:|---:|
| **평균 FPS** | 45.1 | **61.9** | **+37%** |
| **프레임 시간 (중앙값)** | 18.08 ms | **13.29 ms** | **−26%** |
| **프레임 시간 (p95)** | 22.55 ms | **15.33 ms** | **−32%** |
| **드로우콜** | 22,933 | **2,552** | **−89%** |
| **렌더 오브젝트** | 28,251 | **3,287** | **−88%** |
| 렌더 프리미티브 | 8.13 M | 7.90 M | −3% |
| **노드 수** | 8,685 | **2,633** | **−70%** |
| 오브젝트 수 | 14,705 | 6,483 | −56% |
| **정적 메모리** | 221.9 MB | **167.1 MB** | **−25%** |
| 비디오 메모리 | 987.4 MB | 972.2 MB | −1.5% |
| 씬 루트 자식 수 | 3,770 | **757** | **−80%** |
| MultiMeshInstance3D | 2 | 25 | — |

**p95 가 22.55ms → 15.33ms 로 줄어든 것이 체감상 가장 큰 변화다.**
이전에는 프레임이 자주 튀어 16.6ms(60fps) 예산을 넘겼고, 이제는 p95 조차 예산 안에 들어온다.

---

## 항목별 개선 내역

### 1. MultiMesh — 드로우콜의 89%를 걷어낸 주인공

**문제**: 정적 배경이 전부 개별 `MeshInstance3D` 였다.
가장 심한 곳은 **스카이라인 타워** — 링 3겹 × 타워 수 × 5~16층, 각 층이 별도 노드.
도로 타일, 보도블록, 건물 모듈, 소품, 침식 결정, 이끼도 모두 개별 노드였다.

**해결**: `MeshBatcher` (`scripts3d/world/MeshBatcher.gd`)
- `bake(경로, 위치, 회전, 스케일, 색조)` 로 모아두고, 마지막에 `batch_commit()` 에서
  **같은 메시끼리 하나의 `MultiMeshInstance3D`** 로 합친다
- 색조는 MultiMesh 의 **per-instance color** + 재질의 `vertex_color_use_as_albedo`
  → 기존 `_tint_node()` 와 동일한 시각 결과를 유지하면서 배칭이 가능해졌다
- 배경 대량 오브젝트는 `cast_shadow = OFF` — 그림자 패스가 드로우콜을 2배로 만들던 것을 차단
- 모델 파트(메시/로컬 트랜스폼/재질)는 경로별로 **static 캐시** — 같은 .glb 를 반복 파싱하지 않는다

배칭 대상: 도로 · 보도 · 건물 층/지붕 · 부서진 벽체 · 스카이라인 타워 · 경계 잔해 ·
소품(배럴/상자/바위/바리케이드/팔레트) · 나무 · 가로등 · 신호등 · 침식 결정 · 이끼 → **25개 MultiMesh**

**의도된 한계**: MultiMesh 인스턴스는 개별로 지우거나 움직일 수 없다.
그래서 **충돌은 배칭하지 않았다** — 기존처럼 별도 `StaticBody3D` 가 담당하며,
충돌이 필요한 트럭은 개별 노드로 남겼다. 게임플레이 충돌 판정은 전혀 바뀌지 않았다.

### 2. Object Pool — 재사용률 95.3% / 67.4%

**문제**
- 데미지 숫자(`Label3D`)를 **피격마다** new/queue_free. 광역기로 10마리를 때리면 한 프레임에 노드 10개 + Tween 10개 생성, 0.5초 뒤 전부 해제
- 기탄(`Area3D` + 스크립트 + 메시 + 라이트)을 **발사마다** 런타임 조립. 보스 페이즈 3은 한 번에 7발

**해결**: `VfxPool` 오토로드 + `Projectile3D` 의 `_build_parts()` 1회 생성 구조

실전 페이스로 측정한 재사용률:

| 대상 | 조건 | 신규 생성 | 재사용 | 재사용률 |
|---|---|---:|---:|---:|
| 데미지 숫자 | 초당 ~14개, 12초 (콤보 연타) | 8 | 162 | **95.3%** |
| 기탄 | 5발/초, 15초 (보스 탄막) | 28 | 58 | **67.4%** |

기탄 재용률이 낮은 이유는 수명이 4.5초라 최대 28발이 동시에 살아 있기 때문이다 — 정상이다.
풀 상한(데미지 40 / 기탄 48)을 둬서 메모리가 무한히 늘지 않는다.

> 참고: 수명보다 빠르게 생성하는 비현실적 스트레스 테스트(데미지 300개/1.8초)에서는
> 재사용률이 20%로 떨어진다. 동시 생존 수가 풀 상한을 넘으면 새로 만드는 것이 맞는 동작이다.

### 3. Enemy Cache + Node Cache + Group Search 제거

**문제**: 적 1기가 **매 물리 프레임** `get_first_node_in_group()` 을 3회(플레이어/방주/방벽) 호출.
적 50기면 **프레임당 150회 트리 검색**. HUD/미니맵/파티클도 매 프레임 그룹을 훑었다.

**해결**: `Battlefield` 오토로드
- `process_physics_priority = -100` 으로 다른 노드보다 먼저 돌아 **프레임당 1회** 갱신
- 플레이어/방주는 **참조가 무효화될 때만** 다시 찾는다 → 대부분 프레임은 검색 0회
- 적/방벽 목록은 프레임당 1회 수집해 모두가 공유

교체한 호출부: `EnemyMovement`(3곳) · `EnemyBrain` · `PlayerCombat`(3곳) ·
`MiniMap3D`(3곳) · `HUD3D` · `QuestUI` · `Atmosphere3D` · `Altar3D` · `Pet3D` ·
`SurvivorNPC3D` · `PetManager` · `WaveManager3D` · `Enemy3D`

**적 1기당 프레임 검색 3회 → 0회.** 남은 그룹 검색은 웨이브 종료 등 1회성 코드뿐이다.

### 4. Draw Call 감소 — 종합

| 요인 | 절감 |
|---|---|
| MultiMesh 배칭 | 주 요인 (−20,000 이상) |
| 배경 그림자 캐스팅 OFF | 그림자 패스 중복 제거 |
| 랜드마크 링 `visibility_range_end` | 근접 시에만 렌더 |
| 랜드마크 이름 `visibility_range_end = 46m` | 원거리 텍스트 컬링 |

### 5. SubViewport 최적화

초상화/무기 아이콘 뷰포트는 대기 애니메이션이 돌아야 해서 `UPDATE_ALWAYS` 를 유지했다
(78×78 픽셀이라 렌더 비용 자체는 작다). 대신 낭비를 잘라냈다:

```gdscript
vp.positional_shadow_atlas_size = 0     # 작은 UI 뷰포트에 그림자 아틀라스 불필요
vp.msaa_3d = MSAA_DISABLED
vp.screen_space_aa = SCREEN_SPACE_AA_DISABLED
vp.use_debanding = false
```

### 6. Material 공유

**문제**: 적 1기가 생성될 때마다 `flash_mat` 를 새로 만들었다.
같은 값의 재질이 N개면 메모리 낭비 + 렌더러가 배칭할 수 없어 상태 전환이 늘어난다.
VFX도 매번 새 재질을 만들었다.

**해결**: `SharedMaterials` 오토로드
- 공유: 적 피격 플래시(2단계) · 발광 눈알 · 기탄 · 플레이어 플래시
- **개별 사본이 필요한 것**은 `unshaded_fade()` 로 명시적으로 복제
  (공유 재질의 알파를 트윈하면 화면의 같은 VFX가 전부 함께 사라진다 — 이 함정을 코드 주석으로 못 박았다)

정적 메모리 221.9 MB → 167.1 MB 감소에 기여했다.

---

## 남은 병목과 다음 단계

| 순위 | 항목 | 현재 | 처방 |
|---|---|---|---|
| 1 | **비디오 메모리 972 MB** | 거의 줄지 않았다 | `assets3d/` 957MB — Mixamo FBX 원본(24MB×6)과 6.5MB PNG 텍스처가 원인. 텍스처를 2048→1024로 리사이즈하고 FBX 원본을 export 에서 제외 |
| 2 | **프리미티브 7.9 M** | −3%만 감소 | MultiMesh 는 드로우콜을 줄이지만 정점 수는 그대로다. 스카이라인에 LOD 또는 저폴리 프록시 메시 도입 |
| 3 | **동적 OmniLight** | 적당 1~2개(눈/오라) + 가로등 40개 | 눈알은 emission만으로 대체, 오라 라이트는 보스 전용으로 제한 |
| 4 | **적 AI 레이캐스트** | 시야 0.18초 / 회피 0.12초 간격 | 이미 간격 제한됨. 적 100기 이상이면 프레임 분산(라운드로빈) 필요 |
| 5 | **SDFGI / 볼류메트릭 포그** | 켜져 있음 | 저사양 옵션에서 끌 수 있는 그래픽 프리셋 메뉴 |

## 재현 방법

```bash
BENCH_TAG=BEFORE BENCH_BATCH=0 godot --path . --resolution 1280x720
```

`tools/bench.gd` 를 오토로드로 등록하면 위 표와 같은 지표가
`tools/bench_BEFORE.txt` / `tools/bench_AFTER.txt` 로 저장된다.
(측정 도구는 게임 빌드에 포함되지 않는다)
