# ENEMY_AI — EnemyBrain State Machine 설계서

> 기존 구조(EnemyController + Brain/Movement/Attack/Animation)를 유지하면서,
> `EnemyBrain` 내부를 **명시적 State Machine** 으로 재작성했다.

---

## 0. 이전 구조의 문제

`_select_target()` 로 목표 한 점을 고르고 매 프레임 직선으로 달려갔다. 결과:

| 증상 | 원인 |
|---|---|
| 모든 적이 플레이어 **정면 한 점**에 뭉쳤다 | 협공 개념이 없음 |
| 벽 뒤에 있어도 벽에 몸을 비비며 붙었다 | 시야 판정이 없음 |
| 원거리형(비명귀)도 코앞까지 붙었다 | 거리 유지가 없음 |
| 돌진이 헛나가도 같은 각도로 계속 밀어붙였다 | 재배치가 없음 |
| 할 일이 없으면 완전히 정지했다 | Idle 연출이 없음 |
| 보스가 처음부터 끝까지 같은 패턴 | 페이즈 개념이 없음 |

---

## 1. 상태 다이어그램

```
                    ┌──────────┐
        stun ──────▶│ STUNNED  │──── stun 종료 ──┐
                    └──────────┘                 │
                                                 ▼
   ┌────────┐  시야 확보   ┌────────┐  거리 < 18m  ┌────────┐
   │  IDLE  │────────────▶│ CHASE  │────────────▶│ FLANK  │
   └────────┘             └────────┘  (슬롯 확보)  └────────┘
       ▲  ▲                   ▲                       │
       │  │ 기억 만료          │                  너무 붙음
       │  └────────┐          │                       ▼
       │           │          │                  ┌─────────┐
   시야 상실   ┌─────────┐    │                  │ SPACING │
       └──────▶│ SEARCH  │────┘                  └─────────┘
               └─────────┘                            │
                (마지막 목격 위치)                      │
                                                      │
   ┌──────────┐  돌진 예고/돌진 중   ┌──────────────┐  │
   │  WINDUP  │◀──────────────────  │ REPOSITION   │◀─┘
   └──────────┘   헛스윙 보고 ──────▶└──────────────┘
                                     (원호를 그려 다른 각도로)

   ┌────────┐  is_siege == true (플레이어 6m 밖)
   │ SIEGE  │  방벽 → 방주 우선 파괴
   └────────┘
```

전이는 `_decide_state()` **한 곳에서만** 일어난다.
각 상태는 "이번 프레임 어디로 갈지(intent)"만 만들고, 실행은 `EnemyMovement` 가 한다.

```gdscript
# 브레인이 채우는 의도
var move_target: Vector3    # 목표점
var has_move: bool
var speed_mult: float       # 이동 속도 배율
var face_dir: Vector3       # 이동과 다른 방향을 볼 때 (후진하며 응시)
```

---

## 2. 요구 8항목 구현

### ① 주변 적 협공 (Flanking)

`Battlefield` 오토로드가 **슬롯 배분소** 역할을 한다.

- 플레이어 주위를 **8방향(45° 간격)** 으로 나눈다
- 22m 안의 적에게 슬롯을 **1개씩 배타적으로** 배분한다
- **자기 현재 방향에 가장 가까운 빈 슬롯**부터 탐색한다 (0, +1, −1, +2, −2 …)
  → 불필요한 우회 없이 자연스럽게 자리를 잡는다
- 슬롯을 받은 적은 `플레이어 + 슬롯방향 × 선호거리` 를 목표로 삼는다
- 슬롯은 **유지된다** — 매 프레임 재배분하면 경로가 떨린다 (`SLOT_REFRESH = 1.2초`)
- 죽거나 30m 밖으로 나가면 자동 회수 (`_prune_slots`), 노드 소멸 시 `_exit_tree` 에서 반납

**측정 결과** (적 14기 배치 후 4초):
```
states = { FLANK: 5, IDLE: 7, SIEGE: 1, CHASE: 1 }
flank_angles = [0, 45, 90, 270, 315]     ← 서로 다른 5방향에서 접근
```

### ② 플레이어 거리 유지 (Spacing)

타입별 선호 거리:

| 패턴 | 선호 거리 | 근거 |
|---|---|---|
| 근접 (사냥개/파괴자) | 히트박스 + 0.9m | 붙어야 때린다 |
| 돌진 (추적자/저거너트) | 7.5m | 돌진 준비 거리 |
| 원거리 (비명귀) | 11.0m | 사거리(16m) 중간 |
| 보스 | 9.0m | 탄막이 유효한 거리 |

선호거리 × 0.72 보다 가까워지면 `SPACING` 으로 전환해 **뒤로 물러나면서도 플레이어를 응시**한다
(`face_dir = -away`, 속도 0.85배).

**근접 몹은 거리 유지를 하지 않는다** — 기존 위협도를 유지하기 위한 의도적 제외다.
엘리트(tier 1) 돌진형만 예외적으로 거리를 잰다.

### ③ 공격 실패 후 재배치 (Reposition)

`EnemyMovement` 가 돌진 종료 시 명중 여부를 확인하고, 못 맞혔으면 `brain.report_whiff()` 를 호출한다.

- `REPOSITION` 진입 → 현재 각도에서 **좌/우로 66°(1.15rad)** 벌린 원호 지점으로 이동
- 원호 반지름 = 선호거리 × 1.5, 속도 1.15배 (지루하지 않게 조금 빠르게)
- 1.1초간 유지 (엘리트는 0.77초 — 반응이 빠르다)
- **연속 2회 헛스윙하면 회전 방향을 반대로 뒤집는다** → 같은 패턴 반복 방지

### ④ 벽 회피

`EnemyMovement._probe_walls()` — 전방 1개 + 좌/우 2개 레이캐스트 (레이어 1 = 지형만).

| 상황 | 조향 |
|---|---|
| 정면 막힘 + 좌측만 막힘 | 우측으로 크게 |
| 정면 막힘 + 우측만 막힘 | 좌측으로 크게 |
| 정면 막힘 + 양쪽 동일 | `instance_id % 2` 로 **일관된** 방향 (좌우로 떠는 것 방지) |
| 측면만 스침 | 그 반대로 0.5배 살짝 |

- 탐지 거리: 전방 2.4m / 측면 1.8m, 조향 세기 1.35
- **0.12초 간격**으로만 갱신하고 그 사이에는 조향 벡터를 유지 → 비용 절감 + 부드러움
- **비행 몹은 회피하지 않는다** (지형을 넘어간다)

### ⑤ 시야 기반 추적

- 눈 위치 → 플레이어 가슴으로 레이캐스트 (레이어 1만, 자기 제외)
- 30m 밖이면 판정 없이 "안 보임"
- **0.18초 간격**으로만 판정 (매 프레임 레이캐스트는 비싸다)
- 보이면 `last_known_pos` 갱신
- 안 보이면 → `SEARCH` (마지막 목격 위치로 속도 0.8배로 이동)
- **4초(MEMORY_TIME)** 동안 못 찾으면 `IDLE` 로 복귀

**측정**: 적 14기 중 **6기만** `can_see_player = true` — 나머지는 건물에 가려졌다.
이전에는 14기 전부가 벽을 통과해 플레이어를 인지했다.

### ⑥ 랜덤 Idle

`idle_variant` 를 3종 중 무작위로 뽑고 1.2~3.0초마다 갱신한다.

| variant | 행동 |
|---|---|
| 0 | 두리번거리기 — 제자리에서 방향만 바꾼다 |
| 1 | 짧은 배회 — 속도 0.35배로 주변 3m 를 걷는다 |
| 2 | 정지 — 완전히 멈춰 숨을 고른다 |

**왜 필요한가**: 멀리 있는 적이 완전히 정지해 있으면 "게임이 멈춘 것" 처럼 보인다.
살짝 움직이는 것만으로 세계가 살아 있게 느껴진다.

### ⑦ Elite AI (tier 1)

`is_siege == true` 인 파괴자/저거너트가 엘리트다.

| 차이 | 일반 | 엘리트 |
|---|---|---|
| 재배치 시간 | 1.1초 | **0.77초** (더 빨리 다시 붙는다) |
| 거리 유지 | 근접형은 안 함 | **돌진형도 거리를 잰다** |
| 목표 우선순위 | 플레이어 | **방벽 → 방주** (SIEGE 상태) |
| 플레이어 반응 | 즉시 | **6m 안으로 들어오면** 공성을 중단하고 대응 |

마지막 항목이 개선점이다 — 이전 파괴자는 플레이어를 **완전히 무시**해서
플레이어가 옆에서 때려도 방주만 두드렸다. 지금은 가까이 오면 반응한다.

### ⑧ Boss AI 기반 구조 (tier 2)

HP 구간으로 페이즈가 갈리고, 페이즈마다 패턴 파라미터가 달라진다.

| 페이즈 | HP | 탄막 쿨 | 탄 수 (환수/군주) | 돌진 쿨 |
|---|---|---|---|---|
| 0 | 100~66% | 1.50초 | 3 / 5 | 6.0초 |
| 1 | 66~33% | 1.25초 | 4 / 6 | 5.0초 |
| 2 | 33~0% | 1.00초 | 5 / 7 | 4.0초 |

**페이즈 0 수치는 원본과 완전히 동일하다.** 후반 페이즈만 공격적으로 만들었으므로
진입 장벽은 그대로이면서 "거의 다 잡았다"가 몸으로 느껴진다.

새 보스를 추가하려면 `_try_boss_pattern()` 에 분기 하나만 넣으면 된다.

---

## 3. 튜닝 상수 위치

전부 `EnemyConfig.gd` 한 곳에 있다. AI 를 조정할 때 다른 파일을 열 필요가 없다.

```gdscript
SIGHT_RANGE = 30.0        LOS_INTERVAL = 0.18      MEMORY_TIME = 4.0
RANGE_KEEP_MELEE = 0.9    RANGE_KEEP_CHARGE = 7.5
RANGE_KEEP_RANGED = 11.0  RANGE_KEEP_BOSS = 9.0
SPACING_INNER = 0.72      SPACING_TARGET = 1.05    SPACING_SPEED = 0.85
FLANK_RANGE = 18.0        SLOT_REFRESH = 1.2
REPOSITION_TIME = 1.1     REPOSITION_ARC = 1.15    REPOSITION_SPEED = 1.15
IDLE_WANDER_SPEED = 0.35  SEARCH_SPEED = 0.8       SIEGE_INTERRUPT_RANGE = 6.0
AVOID_PROBE = 2.4         AVOID_SIDE_PROBE = 1.8   AVOID_STRENGTH = 1.35
TURN_SPEED = 9.0          ARRIVE_EPSILON = 0.15
```

---

## 4. 성능

새 AI 는 레이캐스트를 쓰지만 **전체 물리 시간은 오히려 개선됐다.**

| 항목 | 이전 | 이후 |
|---|---|---|
| 적 1기당 프레임 그룹 검색 | 3회 | **0회** (`Battlefield` 캐시) |
| 시야 레이캐스트 | — | 0.18초 간격 (초당 5.5회) |
| 회피 레이캐스트 | — | 0.12초 간격 × 3방향 |
| 물리 프레임 시간 (적 44기) | 3.65 ms | 3.96 ms |

물리 시간은 0.3ms 늘었지만, 그룹 검색 제거로 절감된 process 시간이 이를 상회한다
(전체 프레임 중앙값 18.08ms → 13.29ms — 자세한 내용은 `PERFORMANCE_REPORT.md`).

---

## 5. 발견해 고친 버그

**브레인 티어가 잘못 계산되던 문제**

`brain.setup()` 은 `Enemy3D._ready()` 에서 호출되는데, 그 시점에 `enemy_type` 은 아직
기본값 `"grunt"` 이고 `is_siege` 는 `false` 다. 실제 타입은 `Enemy3D.setup(type, wave)` 에서
정해지므로, **보스와 엘리트가 전부 tier 0(일반)로 계산됐다.**

수정: `EnemyBrain.refresh_tier()` 를 분리하고 `Enemy3D.setup()` 끝에서 호출한다.

측정으로 확인:
```
tiers(0=일반, 1=엘리트, 2=보스) = { 0: 12, 1: 1, 2: 1 }
```
파괴자 1기 → 엘리트, 환수 1기 → 보스로 정상 분류.

---

## 6. 다음 단계

| 항목 | 내용 |
|---|---|
| **경로 탐색** | 지금은 레이캐스트 회피(local steering)다. 막다른 골목에서는 헤맬 수 있다. `NavigationRegion3D` 로 바꾸면 완전해지지만 맵이 절차 생성이라 베이크 타이밍 설계가 필요하다 |
| **그룹 전술** | 슬롯은 위치만 나눈다. "동시 돌진" / "포위 후 일제 공격" 같은 협조 타이밍은 없다 |
| **프레임 분산** | 적 100기 이상이면 시야 판정을 라운드로빈으로 나눠야 한다 |
| **소리 인지** | 지금은 시각만 쓴다. 플레이어의 공격 소리로 위치를 추정하는 청각 인지를 넣으면 스텔스 플레이가 가능해진다 |
