# VERTICAL_SLICE — Steam 데모 설계서 (10~15분)

> 목표: 기능을 더 만들지 않고, **이미 있는 시스템으로 완결된 한 판**을 만든다.
> 성공 기준은 클리어율이 아니라 한 문장이다 — **"이 게임 계속 해보고 싶다."**

---

## 0. 설계 원칙

기존 게임은 "균열 5개 봉인 → 최종 군주"의 장기 캠페인이다.
여기서 앞 10분만 잘라내면 **시작도 끝도 없는 조각**이 된다. 그래서 데모는 다음을 지킨다.

| 원칙 | 구현 |
|---|---|
| 데모는 **완결**돼야 한다 | 봉인 목표를 5 → **1**로 낮춰, 첫 보스 격파가 곧 엔딩 |
| 첫 판에 **죽어서 끝나면 안 된다** | 방주 HP 150 → **200** |
| **비어 있는 시간**이 없어야 한다 | 랜드마크 10곳이 낮에 갈 곳을 만든다 |
| 마지막에 **다음이 궁금해야** 한다 | 엔딩 후 남은 봉인 4개 · 미탐험 랜드마크를 보여준다 |
| 새 기능을 만들지 않는다 | `DemoDirector` 가 **기존 시그널에 얹혀** 연출만 호출한다 |

`DemoDirector.DEMO_MODE = false` 로 두면 게임은 원래의 장기 캠페인으로 돌아간다.

---

## 1. 페이싱 표 (실측 기반)

| # | 비트 | 길이 | 누적 | 플레이어가 느껴야 할 것 |
|---|---|---|---|---|
| 1 | **각성 (INTRO)** | 30~90초 | 1.5분 | "내 특성이 뭘까?" — 도박의 설렘 |
| 2 | **튜토리얼** | 60초 | 2.5분 | "조작이 시원하다" |
| 3 | **첫 탐험** | 90초 | 4분 | "저 빛기둥은 뭐지?" |
| 4 | **첫 랜드마크** | 90초 | 5.5분 | "여기 오길 잘했다" |
| 5 | **밤 예고** | 15초 | 6분 | "긴장된다" |
| 6 | **웨이브 1** | 45초 | 6.5분 | "이제 진짜다" |
| 7 | **정비** | 20초 | 7분 | "장비를 갈아야 하나?" |
| 8 | **웨이브 2** | 45초 | 8분 | "벅차다 — 아슬아슬하다" |
| 9 | **첫 보스** | 90~180초 | 11분 | "이겼다!" |
| 10 | **첫 보상** | 26초 | 11.5분 | "강해졌다" |
| 11 | **엔딩** | 40초 | 12분 | "다음 균열은 어디 있지?" |

**고정 구간 합계 = 약 5분 40초.** 나머지는 플레이어의 자유 탐험/전투 시간이므로
실측 **10~15분**에 들어온다. (자동 주행 검증: 낮을 건너뛰면 141초에 전체 아크 완주)

관련 상수 (`DemoDirector`):
```gdscript
DEMO_DAY_DURATION   = 90.0   # 원본 60 → 탐험할 시간을 준다
DEMO_WAVE_DURATION  = 45.0   # 원본 32 → 전투를 충분히 맛보게
DEMO_REST_DURATION  = 20.0
DEMO_WAVES_TONIGHT  = 2      # 2웨이브 뒤 바로 보스
DEMO_SEALS_NEEDED   = 1      # 봉인 1개 = 데모 클리어
DEMO_BASE_HP        = 200.0
```

---

## 2. 비트별 상세 설계

### ① 각성 — 특성 뽑기 (0:00 ~ 1:30)

**이미 있는 것을 그대로 쓴다**: `TraitScreen` — F~SSS 특성, "새로 태어나기(R)", "운명 시험 10만회(T)".

**왜 이게 좋은 오프닝인가**
- 게임을 켜자마자 **선택**이 주어진다. 컷신이 아니라 플레이어의 결정으로 시작한다.
- SSS 0.0001% 라는 확률이 화면에 적혀 있다 → "언젠가 SSS를 뽑고 싶다"는 **장기 동기**가 첫 30초에 심긴다.
- `T` 키의 10만회 시뮬레이션은 그 자체로 재미있는 장난감이다. 이탈률이 가장 높은 첫 1분을 붙잡는다.

**데모 조정**: 없음. 이 화면은 이미 완성도가 높다.

### ② 튜토리얼 — 조작 (1:30 ~ 2:30)

**흐름**: 튜토리얼 패널 → 아무 키 → 안전지대에서 자유 이동.
안전지대(반경 SAFE_RADIUS)에는 적이 스폰되지 않으므로 **안전하게 조작을 익힌다.**

**데모가 얹는 것**
- 첫 아이템 획득 시 → 토스트 `"I 키로 인벤토리를 열어 장착하세요"`
  (`_on_item_collected`, 최초 1회만)
- 이 순간이 곧 "아이템 → 스탯 → 강해짐" 루프의 첫 접점이다

**의도**: 강제 튜토리얼 구간을 만들지 않았다. 플레이어는 이미 걷고 베고 있다.
필요한 정보만 필요한 순간에 준다.

### ③ 첫 탐험 (2:30 ~ 4:00)

**유인 장치 — 이것이 데모의 핵심 발명이다**

랜드마크마다 세워지는 것:
- **수직 광주** (20m, 랜드마크 고유색) — 화면 어디서든 "저기 뭔가 있다"
- **바닥 링** (근접 시에만 렌더) — "여기가 입구다"
- **이름 표식** (46m 안에서만 보임) — "무너진 보건소"
- **미니맵 아이콘** — 미탐험은 **속 빈 링**, 탐험 완료는 **채운 점**, 클리어는 **흰 테두리**

**퀘스트 패널**이 다음 목표를 직접 알려준다:
```
◆ 랜드마크 탐험  0 / 10
  - 미탐험: 무너진 보건소
```

**왜**: 이전 낮 페이즈는 "60초 동안 아이템을 주우세요"였다. 갈 곳이 없으니 지루했다.
지금은 미니맵에 속 빈 링 10개가 보인다. **채우고 싶어진다.**

### ④ 첫 랜드마크 — 무너진 보건소 (4:00 ~ 5:30)

안전지대에서 **가장 가까운** 랜드마크가 tier 1 로 배정된다 (거리순 정렬 배치).

| 요소 | 값 |
|---|---|
| 수호 몬스터 | 사냥개 2기 |
| 확정 보상 | **E급 아이템 1개** |
| 일반 아이템 | 2개 |
| 드랍 운 보정 | +10% |
| BGM | `explore` |
| 클리어 보상 | 마석 +30 |

**진입 순간의 연출** (`LandmarkUI`)
화면 하단에 4.2초간 카드가 뜬다 — 배너(1.5초 한 줄)로는 읽을 시간이 없기 때문이다.
```
무너진 보건소                              ◈ 최초 발견
약품 냄새가 아직 남아 있다
응급 침상이 복도까지 밀려나 있다. 누군가 여기서
마지막까지 사람을 살리려 했다.
```

**왜 이 텍스트인가**: 몬스터를 설명하지 않고 **사라진 사람들**을 설명한다.
"누군가 여기서 마지막까지 사람을 살리려 했다" — 이 한 줄이 세계관을 전투 없이 전달한다.
플레이어는 다음 랜드마크의 이야기가 궁금해진다. **이것이 재방문 동기다.**

### ⑤~⑧ 밤 — 첫 웨이브 (5:30 ~ 8:00)

**밤 전환** (`DemoDirector._on_phase_changed`)
```
🌙 해가 진다 — 균열이 열린다
좌클릭 3타 콤보 · Shift 회피 · F 반로환동     ← 전투 직전에 조작을 다시 알려준다
```

**웨이브 1 (45초)** — 사냥개 위주. 새 AI 덕분에 **정면으로 뭉치지 않고 협공 슬롯 8방향에서 조여든다.**
플레이어는 자연스럽게 "등 뒤를 봐야 한다"를 배운다.

**정비 (20초)** — `I` 로 인벤토리, 합성/강화. `N` 으로 즉시 다음 웨이브.
**이 20초가 데모에서 가장 중요한 침묵이다.** 방금 얻은 장비를 끼고 강해진 것을 확인하는 시간.

**웨이브 2 (45초)** — 추적자/파괴자 합류. 파괴자는 **엘리트 AI(tier 1)** 로
방벽·방주를 우선 노리고, 플레이어가 6m 안으로 오면 그때 반응한다.

### ⑨ 첫 보스 — 차원의 환수 (8:00 ~ 11:00)

**등장 연출** (`DemoDirector._summon_demo_boss`)
```
CombatFeel.screen_flash(붉은색, 0.4)   # 화면이 붉게 번쩍
SoundManager.set_bgm("boss")
배너: "⚠ 차원의 환수가 강림한다"
```
방주 **앞쪽 22m** 에 등장 — 플레이어가 반드시 마주하도록.

**보스 AI 3페이즈** (HP 구간)

| 페이즈 | HP | 탄막 쿨 | 탄 수 | 돌진 쿨 |
|---|---|---|---|---|
| 0 | 100~66% | 1.50초 | 3발 | 6.0초 |
| 1 | 66~33% | 1.25초 | 4발 | 5.0초 |
| 2 | 33~0% | 1.00초 | 5발 | 4.0초 |

**왜 3페이즈인가**: 체력이 깎일수록 격렬해진다 → "거의 다 잡았다"가 **몸으로 느껴진다.**
페이즈 0 수치는 원본과 동일하다. 후반만 공격적으로 만들었으므로 진입 장벽은 그대로다.

**해결한 순서 버그**: 원래 구조는 웨이브 종료(DONE) 3초 뒤 무조건 아침이 온다.
그대로 두면 **보스전 중에 아침이 와서** 보스가 사라졌다.
지금은 보스 스폰 시 `night_timer = 999` 로 밤을 붙잡고, 보스 사망 시 4초로 풀어준다.

**보스 처치 연출**
```
슬로모션 0.85초 (0.20배)
흰 화면 플래시 0.55
카메라 흔들림 0.45 / 0.7초
낮은 피치 kill 사운드 → 0.12초 뒤 ultimate 스팅어
```

### ⑩ 첫 보상 (11:00 ~ 11:30)

```
배너: "◈ 첫 균열을 봉인했다"
슬로모션 1.2초 (0.25배)
확정 드랍: A급 1개 + B급 1개   ← 데모를 끝낸 사람은 반드시 강한 것을 얻는다
마석 +120
```

**왜 확정 A급인가**: 데모의 마지막 인상이 "운이 나빴다"가 되면 안 된다.
그리고 A급 장비를 손에 든 순간 플레이어는 **이걸 쓸 다음 판**을 원하게 된다.

**26초의 여유**를 준 뒤 엔딩으로 넘어간다 — 보상을 줍고 인벤토리를 열어볼 시간이다.

### ⑪ 엔딩 (11:30 ~ 12:00)

기존 `show_victory()` 를 재사용한다 (TRUE ENDING 화면).

**데모용으로 남기는 여운** — 엔딩 화면에 도달할 때 플레이어가 아는 것:
- 봉인 **1 / 5** — 4개가 남았다
- 랜드마크 **2~4 / 10** — 성당과 균열의 심장부는 아직 못 봤다
- 특성은 아직 낮은 등급 — SSS 는 0.0001%
- 방금 얻은 A급 장비를 아직 제대로 못 써봤다

**4개의 미완결이 동시에 열려 있다.** 이것이 "계속 하고 싶다"의 구조적 근거다.

---

## 3. 데모가 실제로 파는 것 (Steam 페이지 관점)

| 훅 | 첫 등장 시점 | 근거 |
|---|---|---|
| **F~SSS 등급 도박** | 0:10 | 오프닝 화면 자체 |
| **10곳의 죽은 도시 탐험** | 2:30 | 미니맵의 속 빈 링 10개 |
| **환경 스토리텔링** | 4:00 | 랜드마크 진입 카드 |
| **협공하는 AI** | 6:30 | 8방향 포위 |
| **3페이즈 보스** | 8:00 | 체력별 격렬함 상승 |
| **타격감** | 2:30 (첫 타격) | 히트스톱·궤적·화면 플래시 |

---

## 4. 검증 결과

`tools/demorun.gd` 로 전체 아크를 자동 주행했다 (time_scale 8배 가속).

```
DEMO| day_duration=90.0 wave_duration=45.0 waves=2 seals_needed=1 base_hp=200.0
DEMO| beat -> LANDMARK @   2.5s
DEMO| beat -> WAVE     @   4.6s
DEMO| beat -> BOSS     @ 114.6s
DEMO| beat -> REWARD   @ 116.2s
DEMO| beat -> OUTRO    @ 141.3s
DEMO| seals=1/1  kills=85  essence=369  inventory=3
DEMO| final_beat=OUTRO
```

- 모든 비트가 **순서대로** 도달 (누락·역행 없음)
- 봉인 **1/1** 정확 (초기에 2/1 로 이중 카운트되던 버그 수정)
- 방주 HP 200 유지 — 플레이어가 방어하면 죽지 않는다
- 낮 90초를 건너뛴 주행이 141초 → **실제 플레이는 10~15분** 범위

### 주행에서 발견해 고친 것

| 문제 | 원인 | 수정 |
|---|---|---|
| 봉인이 2개 쌓임 | `DemoDirector` 와 `_end_night_phase` 가 각각 `add_seal()` | 디렉터가 호출하지 않도록 |
| 보스전 중 아침이 옴 | DONE 상태 3초 뒤 무조건 밤 종료 | 보스 스폰 시 `night_timer = 999` |
| 봉인 목표가 화면에 5로 표시 | `GameManager.SEALS_NEEDED` 하드코딩 | `DemoDirector.seals_needed()` 로 통일 |

---

## 5. 데모 출시 전 남은 작업 (우선순위)

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | **`git init`** | 여전히 버전 관리가 없다. 데모 빌드를 태그할 수 없다 |
| 2 | **BGM 6트랙** | `SoundManager` 에 시스템은 완성됐지만 **음악 파일이 없다.** `assets/music/{day,night,explore,tense,danger,boss}.ogg` 를 넣으면 코드 수정 없이 동작한다. 데모 체감 품질에 가장 큰 영향 |
| 3 | **일시정지 메뉴 + 볼륨 슬라이더** | Steam 데모의 사실상 필수. `SoundManager.set_master_db()` 는 이미 있다 |
| 4 | **텍스처 다운스케일** | 비디오 메모리 972MB. 2048→1024 로 줄이면 저사양 PC 대응 |
| 5 | **Export Preset 필터** | Mixamo FBX 원본(24MB×6)이 빌드에 들어가면 안 된다 |
| 6 | **엔딩 화면 데모 문구** | 지금은 캠페인 TRUE ENDING. "데모는 여기까지입니다 — 위시리스트에 추가" 로 교체 필요 |
| 7 | 그래픽 프리셋 (SDFGI/포그 토글) | 저사양 옵션 |

## 6. 의도적으로 데모에서 뺀 것

- **생존자 구조** — `SurvivorNPC3D` 가 어디서도 스폰되지 않는 죽은 코드다. 데모에서는 등장하지 않으므로 문제되지 않는다. (기능 삭제 금지 원칙에 따라 파일은 보존)
- **펫 해금** — 보스 처치로 해금되는 구조라 데모에서 1종만 체험한다. 의도된 티저.
- **봉인 2~5 / 최종 군주** — 본편의 내용. 데모는 첫 봉인으로 끝난다.
