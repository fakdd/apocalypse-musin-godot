# COMBAT_POLISH — 타격감(Game Feel) 개선 보고서

> 목표: **새 기능·새 스킬·새 적을 추가하지 않고**, 기존 전투가 "때리는 맛"이 나게 만든다.
> 스킬 구성, 쿨다운, 데미지 배율, 콤보 배율은 **하나도 바꾸지 않았다.**
> 바뀐 것은 *입력이 유실되지 않게 하는 장치*와 *같은 사건을 더 잘 전달하는 연출*이다.

---

## 0. 핵심 구조 변화

새 오토로드 2개가 연출의 "단일 권한자"가 되었다.

| 오토로드 | 역할 | 왜 필요했나 |
|---|---|---|
| `CombatFeel` | `Engine.time_scale`(히트스톱·슬로모션), 화면 플래시 | 시간 배율을 여러 곳에서 건드려 서로 덮어쓰고 있었다 |
| `Battlefield` | 플레이어/적/방주 참조 캐시 | 전투 판정마다 그룹 트리 검색을 반복했다 |

새 플레이어 모듈 2개:

| 모듈 | 역할 |
|---|---|
| `PlayerInput` | 어택 버퍼 · 인풋 큐 · 이동 입력 수집 |
| `PlayerSkill` | "지금 이 스킬을 쓸 수 있는가" 판단 게이트 |

---

## 1. Hit Stop — 연타 시 사라지던 히트스톱을 되살렸다

**이전 문제 (실제 버그였다)**
```gdscript
func hitstop(duration, slow):
    if _hitstop_active:
        return          # ← 진행 중이면 새 요청을 통째로 버린다
```
1타 → 2타 → 3타를 빠르게 이으면 1타의 히트스톱이 아직 돌고 있어서
**정작 가장 강한 3타 피니셔의 히트스톱이 무시됐다.** 콤보를 잘 이을수록 타격감이 약해지는 역설.

**개선**
- 요청을 **무게(weight)** 로 받고, 더 강한 요청이 오면 즉시 덮어쓴다(escalate)
- 시간 배율을 `0.035`(거의 정지)로 통일 — 프레임이 "박히는" 느낌
- 무게 프리셋으로 사건별 차등:

| 상황 | 무게 |
|---|---|
| 평타 명중 | 0.035 |
| 콤보 2타 | 0.06 |
| 콤보 3타 (피니셔) | 0.095 |
| 처치 | 0.11 |
| 반로환동 성공 | 0.13 |
| 만천화우 | 0.20 |

`GameManager.hitstop()` 은 호환용 래퍼로 남겨 기존 호출부가 전부 그대로 동작한다.

## 2. Camera Shake — 난수 지글거림 → 트라우마 감쇠 + 방향성 킥

**이전**: `shake_time` 동안 매 프레임 `randf_range(-mag, mag)` 를 h_offset/v_offset 에 대입.
프레임마다 무작위로 튀어서 "카메라가 흔들린다"가 아니라 "화면이 지글거린다"로 보였고,
끝날 때 뚝 끊겼다. 모든 타격이 같은 세기로 흔들려 위기감의 차이가 없었다.

**개선** (`Player3D._update_shake`)
- **trauma 제곱 감쇠** — 강하게 시작해 부드럽게 잦아든다 (선형의 끊김 제거)
- **위상이 다른 사인파 2개 합성** — 난수 대신 연속적인 흔들림
- **방향성 킥** — 맞은 방향/때린 방향으로 카메라가 한 번 밀린 뒤 스프링으로 복귀
  → *어디서* 맞았는지가 화면으로 읽힌다
- **롤(기울기)** — 큰 타격에서만 미세하게 화면이 기운다
- 피해량 비례: `weight = clamp(amount / 25, 0.35, 1.6)`

`shake(mag, dur)` 시그니처는 유지하고 `shake_from(mag, dur, dir)` 를 추가했다.

## 3. Attack Buffer — "눌렀는데 안 나갔다"를 없앴다

**이전**: `if Input.is_mouse_button_pressed(LEFT) and atk_cd <= 0` —
쿨다운이 0.05초 남은 순간의 클릭은 **그냥 사라졌다.** 콤보가 여기서 가장 많이 끊겼다.

**개선** (`PlayerInput`): 클릭의 **눌린 순간(엣지)** 을 `BUFFER_WINDOW = 0.18s` 동안 기억해,
쿨다운이 끝나는 프레임에 즉시 발동한다. 꾹 누르는 연속 공격은 기존과 동일하게 동작한다.

## 4. Input Queue — 공격 모션 중 누른 스킬을 예약한다

**이전**: 공격 모션 중 Shift/E/F 를 누르면 무시됐다.

**개선**: `QUEUE_WINDOW = 0.30s` 동안 **1개만** 예약한다.
모션이 끝나면 예약된 행동이 이어서 나간다.
2개 이상 쌓지 않는 이유 — 조작이 "밀린" 느낌이 나서 오히려 나빠진다.

발동 조건(쿨다운·게이지·밤 전용 등)은 `PlayerSkill` 로 옮겼을 뿐 **값은 원본과 같다.**

## 5. Combo Window — 콤보 상태를 소리와 빛으로 구분

배율(1.0 / 1.25 / 1.9)과 창 길이(0.85초)는 **그대로다.**
바꾼 것은 각 단계가 감각적으로 구분되게 만든 것:

| 콤보 | 타격음 피치 | 히트스톱 | 궤적 색 | 화면 플래시 |
|---|---|---|---|---|
| 1타 | 0.92 (낮게) | 0.035 | 붉은색 | 없음 |
| 2타 | 1.02 | 0.06 | 붉은색 | 없음 |
| 3타 | 1.16 (높게) | 0.095 | 흰금색 | 있음 |

**헛스윙 피드백 추가** — 아무것도 못 맞히면 히트스톱 없이 낮은 바람 소리만 낸다.
이전에는 맞았는지 안 맞았는지 소리로 구분이 안 됐다.

## 6. Hit Flash — 1단계 단색 → 2단계 (백색 → 붉은 잔상)

**이전**: 0.09초 흰색 단색. 큰 타격과 작은 타격이 시각적으로 동일했다.

**개선** (`EnemyAnimation._do_flash(damage_ratio)`)
- 1단계: 순백 0.05초 — 타격이 꽂힌 순간
- 2단계: 붉은 잔상 `0.06~0.18초` (피해 비율에 비례) — 아팠다는 여운
- 재질은 `SharedMaterials` 에서 **공유** (적마다 새로 만들지 않는다)

**데미지 숫자도 피해 비율에 반응한다**: 작은 피해는 작고 노랗게(0.010),
큰 피해는 크고 붉게(0.018) 뜨며 더 높이 솟는다.

## 7. Slash Trail — 부채꼴 "영역" 에 칼날 "궤적" 을 더했다

기존 `_spawn_slash_vfx` 는 베인 **영역**을 보여줬지만 칼이 지나간 **선**이 없어서
스윙이 한 번 번쩍이고 끝났다.

**추가** (`PlayerAnimation.spawn_weapon_trail`)
- 7개 세그먼트를 각도 순으로 배치하고 **시간차(0.055초 간격)로 하나씩 켠다**
  → 칼이 지나가는 *순서*가 눈에 보인다
- 칼끝은 두껍고(0.16) 뒤로 갈수록 얇아진다(0.045) → 속도감
- 3타는 흰금색 + 발광 9.0, 1~2타는 붉은색 + 발광 5.0

## 8. Enemy Hurt Animation — 평타에도 몹이 움찔한다

**이전**: `stun()` 으로만 hit 모션이 재생됐다. 평타를 맞아도 몹이 태연히 걸어와서
"때리는 느낌"이 없었다.

**개선** (`Enemy3D._play_hurt_reaction`)
- 피해 비율에 따라 `0.12~0.30초` 움찔한다
- **경직이 아니다** — 이동을 멈추지 않으므로 난이도는 그대로다
- 공격/돌진 중에는 모션을 끊지 않는다 → "붉은 예고선을 보고 회피" 규칙이 유지된다

## 9. Screen Flash (Critical)

새 크리티컬 **확률 스탯을 만들지 않았다.** 밸런스를 건드리지 않기 위해,
게임에 이미 있는 "결정적 순간"을 크리티컬로 정의했다.

| 순간 | 색 | 강도 |
|---|---|---|
| 콤보 3타 명중 | 흰금색 | 0.16 |
| 적 처치 | 주황빛 | 0.13 |
| 반로환동 성공 | 금색 | 0.30 |
| 만천화우 | 청백색 | 0.42 |
| 큰 피해를 받음 (14 이상) | 붉은색 | 0.20 |
| 보스 강림 / 보스 처치 | 붉은색 / 백색 | 0.40 / 0.55 |

플래시는 `set_ignore_time_scale(true)` 로 재생한다 —
히트스톱으로 시간이 0.035배가 되어도 플래시는 정상 속도로 사라진다.

## 10. Finish Blow Slow Motion

| 상황 | 길이 | 배율 | 이유 |
|---|---|---|---|
| 보스 처치 | 0.85초 | 0.20 | 데모의 클라이맥스 |
| 웨이브 마지막 적 | 0.34초 | 0.30 | 한숨 돌리는 순간을 만든다 |
| 만천화우 | 0.50초 | 0.45 | 광역 섬멸의 여운 |

"마지막 적" 판정은 `Battlefield.enemies` 캐시로 세므로 추가 트리 검색이 없다.
히트스톱이 슬로모션보다 **우선**하도록 `_apply_time_scale()` 에서 우선순위를 정했다 — 둘이 꼬이지 않는다.

## 11. Knockback — 순간이동 → 속도 기반 감쇠

**이전**: `global_position += knockback * 0.05` — 위치를 즉시 더했다. 밀린 것이 **눈에 보이지 않았다.**

**개선**
```gdscript
knock_vel = flat * KNOCKBACK_IMPULSE(2.6)   # 속도에 실어
knock_vel *= 1 - KNOCKBACK_DAMP(11.0) * delta  # 프레임마다 감쇠
```
- 여러 프레임에 걸쳐 밀려나 **"맞고 밀렸다"** 가 읽힌다
- 총 이동거리는 구 방식과 비슷하게 맞춰 **거리 유지 밸런스를 바꾸지 않았다**
- 큰 피해(최대HP 25% 이상)에는 수직 팝(2.2)이 붙는다
- 보스는 저항 0.18배 — 거의 밀리지 않아 위압감이 유지된다

## 12. 사운드 재생 타이밍

| 개선 | 내용 |
|---|---|
| 콤보별 피치 | 1타 0.92 → 3타 1.16. 콤보가 오를수록 음이 높고 두꺼워진다 |
| 다중 명중 볼륨 | 맞은 수에 비례해 최대 +4dB, 피니셔 추가 +2dB |
| **처치음 지연** | `play_delayed("kill", 0.05)` — "베었다(hit) → 죽었다(kill)"가 두 사건으로 들린다 |
| 보스 처치 | 낮은 피치 kill(0.62) + 0.12초 뒤 ultimate 스팅어 |
| **중복 재생 차단** | 같은 소리가 0.04초 안에 겹치면 무시. 광역기 시 진폭이 합쳐져 찢어지던 문제 해결 |
| 헛스윙 | dash 사운드를 0.7 피치 -20dB 로 재활용 (새 에셋 없이 바람 소리) |

`SoundManager` 에 `play_pitched()` / `play_delayed()` 를 추가했다. 기존 `play()` 는 그대로 동작한다.

---

## 검증

```
헤드리스 실행 — 파싱/런타임 에러 0건
적 44기 + 보스 동시 전투에서 60fps 유지 (측정: PERFORMANCE_REPORT.md)
전체 데모 아크 자동 주행 완주 (LANDMARK → WAVE → BOSS → REWARD → OUTRO)
```

## 바꾸지 않은 것 (약속대로)

- 스킬 구성 · 키 배치 · 쿨다운 값
- 데미지 · 콤보 배율 · 공격 범위 · 각도
- 적 HP · 이동속도 · 접촉 피해 · 패턴 발동 거리
- 아이템/등급/드랍 확률
- 방주 HP (데모 페이싱 조정은 `DemoDirector` 가 별도로 담당)

## 남은 개선 여지

1. **무기 본에 실제로 붙는 리본 트레일** — 지금은 고정 각도 세그먼트다. `BoneAttachment3D` 의 칼끝 위치를 프레임마다 샘플링해 `ImmediateMesh` 로 그리면 무기 스킨별 궤적이 달라진다.
2. **히트 정지 중 카메라 미세 줌인** — 피니셔에 FOV 를 1~2도 좁히면 타격이 더 무거워진다.
3. **적별 피격 사운드 분리** — 현재 모든 적이 같은 `hit` 을 쓴다. 골격/거대몹 구분이 있으면 좋다.
4. **컨트롤러 진동** — 입력 계층이 `PlayerInput` 으로 분리됐으니 붙이기 쉬워졌다.
